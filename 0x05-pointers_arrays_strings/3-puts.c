#include "main.h"
/**
* _puts -prints a string, followed by a new linexã
* @str: char
* Return: void
*/
void _puts(char *str)
{
int len;
for (len = 0; s[len] != '\0'; ++len)
;
{
_putchar(s[len])
}
_putchar('\n');
}
