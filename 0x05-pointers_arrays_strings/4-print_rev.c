#include "main.h"
/**
* print_rev -prints a string, in reverse, followed by a new line
* @s: char
* Return: void
*/
void print_rev(char *s)
{
int l;
for (len = 0; s[l] != '\0'; l++)
{
_putchar(str[l]);
}
_putchar('\n');
return;
}
