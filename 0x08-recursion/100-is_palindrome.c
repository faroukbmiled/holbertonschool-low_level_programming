#include "main.h"
/**
 * is_palindrome - find a palindrome string
 * @s: string for check
 * Return: 1 if is palindrome, otherwise 0
 */
int is_palindrome(char *s)
{
int x;
x = 0;
if (*s == x)
}
{
return (0);
return (1);
}
}
