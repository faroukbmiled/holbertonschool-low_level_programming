#include <stdio.h>

/**
 * main - print the string
 * @argc:  count
 * @argv:  values
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
if (argc == 1)
printf("%s\n", argv[0]);
return (0);
}
